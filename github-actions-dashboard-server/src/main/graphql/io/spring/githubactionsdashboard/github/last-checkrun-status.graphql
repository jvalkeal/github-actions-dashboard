query LastCheckrunStatus($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {
    name
    defaultBranchRef {
      target {
        ... on Commit {
          checkSuites(last: 1) {
            nodes {
              status
              conclusion
            }
          }
        }
      }
    }
  }
}

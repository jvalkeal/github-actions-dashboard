# For this local setup use:
# docker run --name test-mariadb -d -p 3308:3306 -e MYSQL_ROOT_PASSWORD=spring -e MYSQL_USER=spring -e MYSQL_PASSWORD=spring -e MYSQL_DATABASE=ghactionsdashboard mariadb:10.4
# docker stop test-mariadb
# docker rm test-mariadb
# docker exec -it test-mariadb mysql -h127.0.0.1 -P3308 -uspring -pspring ghactionsdashboard
spring:
  datasource:
    url: "jdbc:mariadb://localhost:3308/dashboard?useMysqlMetadata=true"
    username: spring
    password: spring
    driver-class-name: org.mariadb.jdbc.Driver
